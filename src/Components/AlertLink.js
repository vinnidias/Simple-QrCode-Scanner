import React from 'react';
import { View, Image, Alert } from 'react-native';
import styles from '../styles';

export default function AlertLink({url}) {
    return (
        <View></View>
        
    )
}